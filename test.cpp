#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	char *s;
	for(int i=0; i<3; i++)
		cin >> s[i];
	int t=atoi(s);
	cout << t+2 << "\n";
	return 0;
}
